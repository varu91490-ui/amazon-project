1. how do we know which product too add to cart when we click add to cart button for this we have a optin in html called 
data attribute:allows us to attach any information to an element
syntax=attributename=attribute value on right and the attribute must start with data


14.modules
a better way to organize the code

to get a variable out of a file
1.add type="module" attribute2.export
3.import

benefits of module:
1.help to avoid naming conflicts like we dont know in which file we have created which variable
2.dont have to worry about order of our files when impotred in html file as if we put data at end it wont work like that and better way to organize our code in large projects

*deduplicating or normalizing the data :
we dont have to store the entire information of a product in another file as it is already saved in product.js and when the same product id is mentioned in the cart.js file it will directly consider the data in product.js file

steps in javascript
1.save the data:example add the deatils or the data to be printed in the arr or in the form of objects
2.use the data to egnerate the html:it is generated in checkout.js 
3.make it interactive

we created utils module to store the common function 
for exapmple converting the price to cents is common in both amazon.js and checkout.js so add this common function

to upate the html
1.use the dom to get the element to remove 
2.use .remove() method to remove from the page

external libraries:code that is outside of our projects
lot of developers write javascript code and put it on internet and we can load it into our project

external librabries let us use code and share code and avoid duplicating work

in the checkout page to update the dates of delivary we have to do
1.get todays dates
2.do calculatins
3.display the date in easy-to-read-format

so we use external library as it takes too much time to do this and the name is DayJs library its 
an famous external library

the javscript code and comprresed as it helps to load and upload fatser and hte process is known as minification

ESM version:
ecmaScript Module ecmascript is nothing but javascript a version that work with javascript modules
nothing but we use esm version of code in javascript to combine both external lobraries and hs together esm is notjing but the code diretly from internet

in checkout.js we re run the same code to update the date when we selcted one instead of dom because in dom it will update all at once and it is not required and here we r doing
1.update the data
2.regenerate all the HTML and this is called MVC
model-view-controller
1.model-saves and manages the data ex:cart.js which manages the data
2.view=takes the data and displays it on the pageex: checkout.js it takes the data and generate html for this
3.controller=runs some code when we interact with the page ex:in the same checkout.js doucument queryselectors does this part to interact with the page
 so MVC makes sure the page always matches the data it is a design pattern